export component SleepTimerPopup {
    in property <bool> is-open;
    in property <int> active-timer; // 0 = off, >0 = minutes
    in property <bool> shutdown-pc;
    callback shutdown-pc-toggled;
    callback timer-selected(int);
    callback custom-timer-requested(string);
    callback controls-moved;

    width: 150px; height: 245px;

    Rectangle {
        width: 100%; height: 100%;
        background: rgba(20, 20, 25, 0.95);
        border-radius: 8px;
        border-width: 1px;
        border-color: rgba(255, 255, 255, 0.1);
        opacity: root.is-open ? 1.0 : 0.0;
        visible: self.opacity > 0.01;
        animate opacity { duration: 150ms; }

        VerticalLayout {
            padding: 8px;
            spacing: 2px;

            for t[idx] in [
                { label: "Off", val: 0 },
                { label: "1 min", val: 1 },
                { label: "5 min", val: 5 },
                { label: "15 min", val: 15 },
                { label: "30 min", val: 30 },
                { label: "60 min", val: 60 }
            ] : Rectangle {
                height: 26px;
                border-radius: 4px;
                background: ta.has_hover ? rgba(255,255,255,0.15) : (root.active-timer == t.val ? rgba(0,200,255,0.2) : transparent);
                
                Text {
                    text: t.label;
                    color: root.active-timer == t.val ? #0cf : #ccc;
                    font-size: 13px;
                    x: 12px;
                    vertical-alignment: center;
                }
                
                ta := TouchArea {
                    mouse-cursor: pointer;
                    clicked => { root.timer-selected(t.val); }
                    pointer-event(evt) => { if evt.kind == PointerEventKind.move { root.controls-moved(); } }
                }
            }

            Rectangle { height: 1px; background: rgba(255,255,255,0.1); }

            HorizontalLayout {
                height: 26px;
                spacing: 4px;
                Rectangle {
                    background: rgba(0, 0, 0, 0.5);
                    border-radius: 4px;
                    border-width: 1px;
                    border-color: custom-ti.has-focus ? #0cf : rgba(255,255,255,0.15);
                    custom-ti := TextInput {
                        text: "";
                        color: #fff;
                        font-size: 13px;
                        vertical-alignment: center;
                        horizontal-alignment: center;
                        single-line: true;
                        accepted => { root.custom-timer-requested(self.text); }
                    }
                }
                Rectangle {
                    width: 36px;
                    border-radius: 4px;
                    background: set-ta.has_hover ? rgba(255,255,255,0.2) : rgba(255,255,255,0.1);
                    Text { text: "Set"; color: #fff; font-size: 12px; horizontal-alignment: center; vertical-alignment: center; }
                    set-ta := TouchArea {
                        mouse-cursor: pointer;
                        clicked => { root.custom-timer-requested(custom-ti.text); }
                    }
                }
            }

            Rectangle { height: 1px; background: rgba(255,255,255,0.1); }

            HorizontalLayout {
                height: 24px;
                padding-top: 4px;
                spacing: 6px;
                alignment: start;
                
                Rectangle {
                    width: 14px; height: 14px;
                    border-radius: 3px;
                    border-width: 1px;
                    border-color: root.shutdown-pc ? #0cf : rgba(255, 255, 255, 0.4);
                    background: root.shutdown-pc ? #0cf : transparent;
                    y: (parent.height - self.height) / 2;
                    
                    if root.shutdown-pc : Path {
                        width: 10px; height: 10px;
                        stroke: #000;
                        stroke-width: 1.5px;
                        fill: transparent;
                        commands: "M 2 5 L 4 7 L 8 2";
                        x: (parent.width - self.width) / 2;
                        y: (parent.height - self.height) / 2;
                    }
                }
                Text {
                    text: "Shutdown PC";
                    color: root.shutdown-pc ? #fff : #aaa;
                    font-size: 11px;
                    vertical-alignment: center;
                }
                
                TouchArea {
                    mouse-cursor: pointer;
                    clicked => { root.shutdown-pc-toggled(); }
                }
            }
        }
    }
}
